from filestorage import StorageContainer as StorageContainer, store as store
from filestorage.config_utils import setup_from_settings as setup_from_settings
from typing import Any

def includeme(config: Any): ...
